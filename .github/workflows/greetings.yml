name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Thanks for contributing! 😁  But if you know the answer to this problem, it is better to do a pull request. Also, make sure you installed all the dependancies and run the script on python3.8 before filing an issue.'
        pr-message: 'Want to contribute? Email me at bryan.hu.cn@gmail.com .'
